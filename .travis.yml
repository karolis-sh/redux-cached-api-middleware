language: node_js
node_js:
  - '10'
  - '9'
  - '8'
cache:
  yarn: true
  directories:
    - node_modules
script:
  - yarn build
  - yarn qa
after_success:
  - travis-deploy-once && codecov
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GH_TOKEN
  keep-history: true
  on:
    branch: master
  local-dir: dist
